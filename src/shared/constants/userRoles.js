import {PATHS} from "../../utils/constants";

const {OPERATORS, CERTIFICATES, NOTIFICATIONS, CLIENTS, SITES, PREFERENCES, MYCOMPANY} = PATHS

export const USER_ROLES = {
    TEST: {
        key: 'TEST',
        access: [
            OPERATORS.INDEX, OPERATORS.CREATE, OPERATORS.EDIT, OPERATORS.SHOW,
            CERTIFICATES.INDEX, CERTIFICATES.CREATE, CERTIFICATES.EDIT, CERTIFICATES.SHOW,
            CLIENTS.INDEX, CLIENTS.CREATE, CLIENTS.EDIT, CLIENTS.SHOW,
            SITES.INDEX, SITES.CREATE, SITES.EDIT, SITES.SHOW,
            NOTIFICATIONS.INDEX,
            MYCOMPANY.INDEX, MYCOMPANY.EDIT,
            PREFERENCES
        ]
    },
    PLANER: {
        key: 'PLANER',
        access: [
            OPERATORS.INDEX, OPERATORS.CREATE, OPERATORS.EDIT, OPERATORS.SHOW,
            NOTIFICATIONS.INDEX,
            CERTIFICATES.INDEX, CERTIFICATES.SHOW,
            CLIENTS.INDEX, CLIENTS.SHOW,
            SITES.INDEX, SITES.SHOW,
            PREFERENCES
        ]
    },
    CLIENT: {
        key: 'CLIENT',
        access: [
            SITES.INDEX, SITES.CREATE, SITES.EDIT, SITES.SHOW,
            NOTIFICATIONS.INDEX,
            MYCOMPANY.INDEX, MYCOMPANY.EDIT,
            PREFERENCES
        ]
    },
    ISSUER: {
        key: 'ISSUER',
        access: [
            CERTIFICATES.INDEX, CERTIFICATES.CREATE, CERTIFICATES.EDIT, CERTIFICATES.SHOW,
            NOTIFICATIONS.INDEX,
            MYCOMPANY.INDEX, MYCOMPANY.EDIT,
            PREFERENCES
        ]
    }
}
