import {PATHS} from "../../utils/constants";

const {OPERATORS, CERTIFICATES, NOTIFICATIONS, DEPARTAMENTS, ROLES, PREFERENCES, MYCOMPANY} = PATHS

export const USER_ROLES = {
    TEST: {
        key: 'TEST',
        access: [
            OPERATORS.INDEX, OPERATORS.CREATE, OPERATORS.EDIT, OPERATORS.SHOW,
            CERTIFICATES.INDEX, CERTIFICATES.CREATE, CERTIFICATES.EDIT, CERTIFICATES.SHOW,
            DEPARTAMENTS.INDEX, DEPARTAMENTS.CREATE, DEPARTAMENTS.EDIT, DEPARTAMENTS.SHOW,
            ROLES.INDEX, ROLES.CREATE, ROLES.EDIT, ROLES.SHOW,
            NOTIFICATIONS.INDEX,
            MYCOMPANY.INDEX, MYCOMPANY.EDIT,
            PREFERENCES
        ]
    },
    PLANER: {
        key: 'PLANER',
        access: [
            OPERATORS.INDEX, OPERATORS.CREATE, OPERATORS.EDIT, OPERATORS.SHOW,
            NOTIFICATIONS.INDEX,
            CERTIFICATES.INDEX, CERTIFICATES.SHOW,
            DEPARTAMENTS.INDEX, DEPARTAMENTS.SHOW,
            ROLES.INDEX, ROLES.SHOW,
            PREFERENCES
        ]
    },
    CLIENT: {
        key: 'CLIENT',
        access: [
            ROLES.INDEX, ROLES.CREATE, ROLES.EDIT, ROLES.SHOW,
            DEPARTAMENTS.INDEX, DEPARTAMENTS.CREATE, DEPARTAMENTS.EDIT, DEPARTAMENTS.SHOW,
            CERTIFICATES.INDEX, CERTIFICATES.CREATE, CERTIFICATES.EDIT, CERTIFICATES.SHOW,
            NOTIFICATIONS.INDEX,
            MYCOMPANY.INDEX, MYCOMPANY.EDIT,
            PREFERENCES
        ]
    },
    // ISSUER: {
    //     key: 'ISSUER',
    //     access: [
    //         CERTIFICATES.INDEX, CERTIFICATES.CREATE, CERTIFICATES.EDIT, CERTIFICATES.SHOW,
    //         NOTIFICATIONS.INDEX,
    //         MYCOMPANY.INDEX, MYCOMPANY.EDIT,
    //         PREFERENCES
    //     ]
    // }
}
